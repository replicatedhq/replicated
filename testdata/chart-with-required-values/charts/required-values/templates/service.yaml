apiVersion: v1
kind: Service
metadata:
  name: {{ include "required-values.fullname" . }}
  labels:
    {{- include "required-values.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "required-values.selectorLabels" . | nindent 4 }}
