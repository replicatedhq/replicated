apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "required-values.fullname" . }}-test-connection"
  labels:
    {{- include "required-values.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "required-values.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
