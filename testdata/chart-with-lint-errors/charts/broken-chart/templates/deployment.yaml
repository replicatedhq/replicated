apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "broken-chart.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "broken-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "broken-chart.name" . }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 80

